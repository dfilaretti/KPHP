/*************************************************************
 * Example with conditional. 
 * 
 * In this case x is initialised to POS, hence it cannot  
 * be equal to a negative. For this reason the analysis only
 * takes the false branch of the conditional
 * 
 *************************************************************/

x = 2;
if (x == -2) then { 
    x = -1;
}
else {
    x = 1213;
}
