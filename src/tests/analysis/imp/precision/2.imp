// { . }
x = input_int();
// { x |-> Top }
if (x < 0) then {
    // { x |-> Neg }
    z = x;
    // { x |-> Neg, z |-> Neg }
}
else {
    // { x |-> Top }
    q = x;
    // { x |-> Top, q |-> Top }
}
// { x |-> Top, z |-> Neg, q |-> Top }


